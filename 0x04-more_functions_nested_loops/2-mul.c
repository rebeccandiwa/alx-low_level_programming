#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with first number
 * Return: The result of multplication between a paran and b paran
 */

int mul(int a, int b)
{
	return (a * b);
}
